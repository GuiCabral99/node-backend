// import fastify from "fastify";
// import { AppointmentRoute } from "./routes/AppointmentRoutes";

// const app = fastify();

// app.register(AppointmentRoute);

// app.get("/", async (request, reply) => {
//   reply.send("Rota home");
// });

// export default app;
